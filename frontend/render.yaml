services:
  - type: web
    name: clokify-frontend
    env: static
    region: oregon
    branch: main
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html